# Kimodo studio activation

The user explicitly approved up to $10 additional Runpod spend on September 13, 2026. Migration `20260913000320` adds a separate service-owned allowance of 1,000 cents. Integration capacity and overall admission increase by the allowance; prior limits and the six existing reservations are preserved. The historical holds total $25. The closed-window Runpod Kimodo report currently returns $0.05546718277037144 for September 9, but the previously observed billing discrepancy remains unresolved, so the old holds were not cleared.

An isolated fast-forward commit `3dc49ca3e1c268f569d419c29ff2e65f343fc771` on the endpoint's existing build branch carries only the native handler/schema update and its agent documentation. This avoids publishing unrelated workspace changes. Five Python native tests passed and a compiled flexible `custom` request passed native validation. The budget migration passed a rollback test allowing exactly the additional $10 and rejecting one cent more, with no authenticated RPC execution permission.

`scripts/animation-studio-live.mjs` uses the existing marked test project. Its plan mode creates a single relaxed-idle graph and runs a real semantic review without app-credit deductions. Its generation mode checks the matching service-owned report, compiles an exact recipe receipt and reserves $2 for one GPU clip under the new allowance. Attempts are recorded before submission; uncertain attempts require reconciliation, never blind resubmission. Status mode downloads successful GLBs and verifies their recorded hashes. Candidates are not automatically accepted or bound.

The fixture readiness review completed successfully. Native image `3dc49ca3e` built and deployed. A first fixture attempt failed before submission because Fly lacked `RUNPOD_GRAPHCORE`; the existing server credential is now configured. Only that proven unsubmitted reservation was released. Retry job `5fd8e50b-f7c7-4d71-864b-c1c1c158035b` completed fresh inference and all CPU stages. Candidate `28fa8e79-2b12-44ae-8ba9-0cce07e8e36a` passed validation and a hash-checked GLB download. It remains pending creator visual review, not accepted or bound.

Both Edge generation flags are enabled for the existing account allowlist. Endpoint settings are min workers 0, max workers 1, idle timeout 5 seconds, execution timeout 600 seconds. There is no always-on worker minimum. The reservation is $2 per clip; $8 of the additional allowance remains uncommitted after the successful test. Original holds are unchanged. TypeScript, production build, native tests and a dev-server browser route check passed with no page errors. Runpod charges worker startup/loading, execution and idle time, not a fixed price per animation. The $2 reservation is a conservative admission hold, not an actual price quote.
