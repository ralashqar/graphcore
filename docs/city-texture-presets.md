# City texture presets

V3 recipes optionally store textures.wall, textures.roof and textures.ground as allowlisted IDs. Omitted/none retains palette shading. Natural material colours override palette tint on selected surfaces. Glass, signs and trim retain separate materials. Wall roles currently use palette colour identity; ground uses low slab height and imported paving roles. This does not retexture city roads.

Eight CC0 ambientCG colour/roughness sets are hosted locally as 512px WebP, approximately 400KB combined. See public/city/textures/sources.json. No geometry displacement, normal-map downloads or live provider requests. Packed surface maps described below provide shading relief. Shared materials load selected maps lazily, use world-space triplanar mapping with mipmaps/repeat/anisotropy, and retain neutral shaded fallback until both maps load. No arbitrary texture URLs. Exported Quaternius wall/paving material roles are supported.

Branding > Surface textures provides independent wall, roof and ground selectors and swatches. Seeded demo designs exercise the collection. Palette-only remains available; existing saved recipes are unchanged. Saving reuses the existing ownership/revision/publication flow, with a matching strict server validator. No migration.

Local verification: TypeScript, build, Deno building-design schema tests, driving shader/browser checks, texture selector and mocked save/reload browser checks. Physical mobile performance and hosted publication remain unverified.

Staging rollout pending designated identifiers. Pair the frontend with affected City Edge endpoints (city-api, city-command, city-building-art, city-stripe-webhook, city-reconcile and any transitive consumers of _shared/city.ts) plus world-generation worker: city-campus-worker imports parseProfile from city.ts. Worker version city-design-3.5. No production activation or paid generation authorized/included.

Texture repeats are enlarged fourfold for street-level readability: brick repeats every 5 world metres, concrete every 12, and other materials every 8. Colour and roughness use the same scale on procedural and Quaternius surfaces; roads are unchanged.

Optional wallBorder and groundBorder IDs separate Quaternius trim and plot/enclosure edges from primary surfaces. Omitted/primary inherits the primary texture; none uses the palette. Demo borders always select a distinct explicit material. Local save/reload and strict schema coverage include inheritance. Shared contract rollout requires the same City Edge/frontend/world-worker staging pair; worker city-design-3.6. No migration or production deployment.

Surface shading now packs source CC0 displacement (R) and roughness (G) in one lossless 512px Surface map. It replaces the runtime roughness image: still two maps and six triplanar samples per fragment, shared by instanced batches. Derivative bump perturbs normals without geometry displacement, fading out with distance/pixel footprint. Metal uses 0.65 metalness; other surfaces retain dielectric highlights controlled by source roughness. Glass remains separate. No new recipe fields or server deployment required for this shading update.

Quaternius native materials: decorator export v2 retains authored colour/normal/metallic-roughness maps and original UVs for solid surfaces. Offline resize bounds every embedded image to 512px (6.2MB complete pack); native normal intensity is reduced to 0.35 and maps are shared with the cached pack. Original / palette selects native materials on kit parts, palette colours on procedural parts; explicit texture IDs still override primary/border roles. Demo facade finishes use native wall and trim by default. Fake interiors remain simplified glass. Rebuild with Blender scripts/build-city-decorators.py, then node scripts/optimise-city-decorators.mjs (includes Pillow resize). No server contract change.

Quaternius architectural assembly (decorator pack v3): native narrow family-specific pilasters frame exposed convex corners and separate facade bays; brick windows vary deterministically, brick plinth bays use base-trim modules and metal ground floors use storefront windows. Parapet roofs receive a single supported cornice course. Uniform scaling, entrance/sign clearance, detail scopes and plot bounds are checked before adding pieces. Columns/cornices are near-only; middle-distance facades retain the same bay layout. Procedural finishes stay unchanged. Expanded pack is approximately 6.3MB. No profile schema changes.

Exploratory 400-property fixture, desktop Chromium while local build tasks were running: 76 close-view draw calls, 2.03M close-view triangles, 25 textures, p50 16.7ms/p95 66.8ms. Mobile-sized viewport on the same desktop GPU: 70 close-view calls, 1.94M triangles, p95 66.6ms. This is not an isolated baseline comparison or physical-mobile acceptance; no performance-regression claim is made.

Native side-wall/step extensions (pack v4): optional solidSideWalls replaces side windows with whole native panel bays. Brick solids now use full-height Brick_Plain_3 rather than three short stacked tiles. Optional stairExtension none/concrete/marble places one bounded source entrance-step module and raised service door against a clear ground-floor side wall; backing bays become solid and conflicting pilasters are suppressed. Unavailable placements retain the authored choice and expose a reason. Decorative only, no traversable gameplay or interiors. Demo selection is deterministic. Shared schema/City frontend and endpoints/world worker city-design-3.7 require matching staging rollout; identifiers still pending, no production deploy or migration.

Native exterior shell (pack v5): facade mode now partitions each exposed wall into non-overlapping native window, plain infill and door regions. Window geometry scales uniformly; only plain wall/fascia/floor pieces use explicit fitted axis scales. Native L-shaped corner returns reserve space on both adjoining faces when dimensions allow; smaller corners retain narrow native pilasters. Family-specific windows, shopfronts, door frames and a fitted native arch for supported porticos expand the catalogue. Native floor tiles replace upper slabs and native fascia replaces visible generated bands after assets load. Wall backing is hidden only when all required assets for that wall are available. Low-detail/loading geometry, foundations and unsupported roofs remain procedural. This is an exterior shell with floor plates, not navigable/furnished interiors. No saved recipe or server schema change.

Pack-v5 exploratory 400-property run (concurrent local compilation): desktop close view 103 calls / 1.79M triangles / 28 textures, p50 16.8ms / p95 150ms; mobile-sized desktop viewport 97 calls / 1.69M triangles, p95 50ms. Triangle count decreased but draw-call variety increased. Timing is noisy and is not an isolated regression comparison or physical-mobile validation; broad performance acceptance remains open.
